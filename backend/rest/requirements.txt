
pip install gunicorn

#Run your Flask application using gunicorn:

gunicorn -w 4 -b 0.0.0.0:5000 run:app

################################
-w 4: Specifies the number of worker processes. Adjust this based on your server's CPU cores.


